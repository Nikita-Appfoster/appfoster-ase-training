// we can use this
var obj = {
    ready: 9,
    when: 4,
    'you are': 15,
};
var arr = [
    9,
    4,
    15,
]

//for small objects and arrays
var obj = { ready: 9, when: 4, 'you are': 15 };
var arr = [ 9, 4, 15 ];


//space after each comma
alp= [ 'a', 'b' ];
new( arg1, arg2 ); 

//correct space inside array/object access
let prop = object[ 'default' ]; 
let firstElement = array[ 0 ]; 

//space around operator
let result = a + b; 

//if -else syntax

var i;

if (num > 3 ) {  // openneing brace is after condition block on the same line 
    console.log( "It is largest" );//space 
} 
 else {
    console.log( "You are largest" );
} // closing } should always on next line 

while ( a < 10 ) {
    a++;
}

for ( var i = 0; i < 10; i++ ) {
    console.log( i );
}
// ternanry operator
let score = ( score >= 60) ? 'Pass' : 'Fail';

// conevbtion for using var 
var a = true;
    var b = false;
        var c = null;

// variable name must be in camel case 

let firstName = "Nikita";
let lastName = "Sah";

// class anme should be in upper camel cas e

// static method shoukd be in camelCase

// Comments come before the code to which they refer, and should always be preceded by a blank line and also capitalize the first letter of the comment 

// === must be use in favor of abstract quality checks 

// Use Single - quotes for string literals 
let myStr = 'strings should be contained in single quotes';

// When a string contains single quotes, they need to be escaped with a backslash (\):
let str2 = 'Note the backslash before the \'single quotes\'';

// method to create arrya and object
var myArray = [ 1, 'WordPress', 2, 'Blog' ];

